# ─── Development Profile ─────────────────────────────────
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/netserve_auth
    username: netserve
    password: netserve_secret

  jpa:
    hibernate:
      ddl-auto: update

  kafka:
    bootstrap-servers: localhost:9092

  data:
    redis:
      host: localhost
      port: 6379
      password: netserve_redis

  # Gmail SMTP — Use an App Password (not your login password)
  # Generate one at: https://myaccount.google.com/apppasswords
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GMAIL_USERNAME:hiroshi.kun4233@gmail.com}
    password: ${GMAIL_APP_PASSWORD:ihbm yevi qpyc igjb}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enabled: true
            required: true

netserve:
  jwt:
    secret: netserve-jwt-secret-key-that-is-at-least-256-bits-long-for-hs256
  app:
    frontend-url: http://localhost:3000

logging:
  level:
    "[com.netserve]": DEBUG
    "[org.springframework.security]": DEBUG
